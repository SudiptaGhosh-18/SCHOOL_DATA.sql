CREATE TABLE STD(
ROLL INT PRIMARY KEY,
STN VARCHAR(100),
AD VARCHAR(100),
DTNO NUMERIC(10)
);
SELECT * FROM STD;

CREATE TABLE MARKS(
ROLL INT PRIMARY KEY,
MRK NUMERIC(10),
SUBID VARCHAR(50)
);
SELECT * FROM MARKS;

SELECT * FROM DEPT;
CREATE TABLE DEPT(
DTN INT PRIMARY KEY,
DN VARCHAR(100),
LOC LOCATION
);








